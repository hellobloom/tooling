# Tooling

Bloom's ESlint and Prettier configurations

## [@bloomprotocol/eslint-config](./packages/eslint-config)

Base ESLint configureation to be used by any project without React

## [@bloomprotocol/eslint-config-react](./packages/eslint-config-react)

Base ESLint configuration to be used by any project with React

## [@bloomprotocol/prettier-config](./packages/prettier-config)

Base Prettier configuration to be used by any project
